# Changelog

## 0.0.3 - 2026-08-09

这个版本继续聚焦阅读状态稳定性，修复快速切换 PDF、恢复阅读位置，以及文库操作并发时可能出现的错位。

1. **PDF 切换**：快速连续打开多个 PDF 时，阅读器只采用最后一次选择的文档，较早的加载、渲染或错误稍后完成也不会替换当前内容。
2. **阅读位置**：切换、重新打开或返回 PDF 时，页码与缩放位置会跟随当前文档，延迟保存不会把旧位置写到另一份文档。
3. **文库一致性**：导入、打开、更新 URL、删除或清空文库同时发生时，最新操作会保持当前阅读内容和本地文库一致，已失效任务不会覆盖较新的内容或恢复已删除数据。

## 0.0.2 - 2026-08-07

这个版本聚焦稳定性，修复快速连续导入和中文输入法重命名阅读设置项时的状态问题。

1. **连续导入**：快速连续导入 Markdown 或 PDF 时，阅读页会保持最后一次操作选择的文档，较早任务稍后完成也不会覆盖它。
2. **过期错误**：较早的导入任务稍后失败时，不再显示已经失效的错误或重新打开操作面板。
3. **中文输入法**：使用拼音等输入法重命名阅读预设或本地字体时，按 Enter 选字不会提前保存未完成的名称。

## 0.0.1 - 2026-08-05

miru 的首个公开版本，提供浏览器本地、阅读优先的 Markdown 与 PDF 阅读体验。

1. **导入与阅读**：支持粘贴、拖放、本地文件和允许跨域访问的 URL，并可阅读 CommonMark、GFM 表格、任务列表和带语法高亮的代码块。
2. **阅读工具**：提供大纲导航、标题折叠、文内搜索、书签、阅读进度恢复和适合打印的版式。
3. **本地文库**：文档、阅读位置和相关元数据均保存在当前浏览器中，重复导入 URL 时会更新已有条目。
4. **PDF 阅读**：支持本地 PDF、连续滚动、页码控制、文本搜索和跨浏览器持久化。
5. **阅读设置**：可调整排版、主题、配色和阅读预设，也可使用可选字体或上传本地字体。
6. **离线与隐私**：应用可安装并能离线启动，不上传用户文档，不添加分析或遥测，用户内容也不会进入共享预缓存。
7. **性能与易用性**：优化长文启动、搜索和 PDF 渲染，并完善键盘焦点、对比度、移动布局和减少动态效果支持。
